---
description: PDFtoBPMN v2.1 — Project Rules entrypoint
globs:
  - "**/*"
alwaysApply: true
---

# PDFtoBPMN v2.1

Источник истины: `.cursor/rules/*.mdc` (модульно, human-only changes).

## Агенты (5, D-011 supersedes D-005)
- **orchestrator** — модель основного чата. Планирует, назначает уровень риска, ревьюит. Код запрещён.
- **coder** — Grok 4.7. Пишет код по плану. Архитектура запрещена.
- **validator** — GPT-5.6 Sol (другое семейство, чем coder). Pre/post gate проверки. Read-only.
- **scribe** — Composer 2.5 Fast. Docs, журнал действий, Gold Standard. Production код запрещён.
- **extractor** — Claude Opus 5.5, temp 0.0. LLM-extraction артефактов из текста СМК. Вызывается orchestrator'ом.

## Архитектура (кратко, D-001/D-009/D-010)
- Один pipeline: ingestion → typed extraction → graph population + text indexing (D-009).
- Organizational Knowledge Graph — единственный SSOT (D-001).
- BPMN, RACI, KPI, Control — views из graph queries (D-010).
- Typed extraction: 13 типов артефактов с provenance.
- Document authority: canonical > draft > superseded.
- Business Studio sync через GUID matching.

## LLM
- Cursor: модели субагентов — в `.cursor/agents/*.md` (поле `model`).
- Batch: Claude API (`claude-sonnet-4-6`).
- Графика: Qwen2.5-VL-7B (локально).

## State (память проекта)
- `docs/DECISIONS.md` — что решили и почему (append-only).
- `docs/CURRENT_STATE.md` — где мы сейчас.
- `.cursor/kg/events.jsonl` — журнал действий агентов (lite KG): `python3 .cursor/state/kg.py add|query|export|import-docs`.
- Scribe ведёт state. Orchestrator читает перед планированием.
