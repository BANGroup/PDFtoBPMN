# ✅ Чеклист быстрого запуска OCR

> Пошаговая инструкция для запуска OCR сервиса и обработки документов

---

## 📋 Предварительные проверки

**Перед запуском убедитесь:**

- [ ] GPU доступна: `nvidia-smi`
- [ ] CUDA работает: `python -c "import torch; print(torch.cuda.is_available())"`
- [ ] venv активирован: `source venv/bin/activate`
- [ ] Модель загружена: `ls ~/.cache/huggingface/hub/models--deepseek-ai--DeepSeek-OCR`
- [ ] Порт 8000 свободен: `ss -tuln | grep 8000` (должно быть пусто)

---

## 🚀 Запуск (3 шага)

### ШАГ 1: Запустить OCR сервис

**Терминал 1:**
```bash
cd $PROJECT_ROOT  # Перейти в корень проекта
source venv/bin/activate
python -m uvicorn scripts.pdf_to_context.ocr_service.app:app --host 0.0.0.0 --port 8000
```

**Ожидаем вывод:**
```
✅ DeepSeek-OCR успешно загружен!
INFO: Uvicorn running on http://0.0.0.0:8000
```

**Время загрузки:** 90-120 секунд (первый запуск)

---

### ШАГ 2: Проверить готовность

**Терминал 2:**
```bash
cd $PROJECT_ROOT
source venv/bin/activate
python3 scripts/utils/check_ocr_health.py
```

**Ожидаемый вывод:**
```
✅ OCR сервис готов к работе
```

**Альтернатива (curl):**
```bash
curl -s http://localhost:8000/health
# {"status":"healthy","model_loaded":true,"cuda_available":true}
```

---

### ШАГ 3: Обработать документ

**Терминал 2 или 3:**
```bash
cd $PROJECT_ROOT
source venv/bin/activate
./run_ocr input/document.pdf output/document_OCR.md
```

**Проверка результата:**
```bash
# Должно быть > 0
grep "Обработано OCR:" output/document_OCR.md
```

---

## ❌ Если что-то пошло не так

### Проблема: Connection refused при обработке

**Причина:** OCR сервис не запущен или не готов

**Решение:**
1. Проверить: `python3 scripts/utils/check_ocr_health.py`
2. Если сервис не работает → вернуться к ШАГ 1
3. Если модель загружается → подождать еще 30-60 сек

---

### Проблема: ModuleNotFoundError при запуске сервиса

**Причина:** Абсолютные импорты в app.py

**Решение:**
1. Запустить smoke test: `bash scripts/tests/smoke_test.sh`
2. Если есть ошибки → исправить по подсказкам
3. Проверить: `grep "from pdf_to_context" scripts/pdf_to_context/ocr_service/app.py`
4. Должно быть пусто (только относительные `from .prompts`)

---

### Проблема: CUDA out of memory

**Причина:** Недостаточно VRAM на GPU

**Решение:**
1. Проверить: `nvidia-smi` → свободная память
2. Закрыть другие процессы использующие GPU
3. Перезапустить сервис

---

## 🔄 Остановка и перезапуск

**Остановка сервиса:**
```bash
pkill -f "uvicorn.*ocr_service"
```

**Перезапуск:**
```bash
# Остановить
pkill -f "uvicorn.*ocr_service"

# Подождать
sleep 2

# Запустить заново (ШАГ 1)
python -m uvicorn scripts.pdf_to_context.ocr_service.app:app --host 0.0.0.0 --port 8000
```

---

## 📊 Мониторинг

**Проверка что сервис работает:**
```bash
ps aux | grep "uvicorn.*ocr_service" | grep -v grep
```

**Просмотр логов (если запущен через nohup):**
```bash
tail -f ocr_service.log
```

---

**Детальная документация:** `docs/DeepSeek_OCR_Guide.md`

