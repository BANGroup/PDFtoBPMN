# Стратегия распознавания BPMN диаграмм с DeepSeek-OCR

**Дата:** 29.10.2025  
**Версия:** 1.0  
**Статус:** Готово к тестированию

---

## 🎯 Цель

Обеспечить **машиночитаемое** извлечение контента из BPMN диаграмм и других графических элементов документов для последующего построения BPMN XML схем.

---

## 📊 Анализ исходных данных

### Документ: КД-СТ-161-01 (Эталон для ознакомления).pdf

**Структура:**
- Всего страниц: 54
- Страниц с BPMN контентом: 43 (79.6%)
- Страниц со сложными диаграммами: 34 (>30 векторных элементов)

**Типы контента:**
1. **Простые BPMN диаграммы** (пример: страница 54)
   - Элементы: Процессы, События, Шлюзы
   - Четкие подписи
   - Линейная структура

2. **Сложные схемы** (пример: страница 26)
   - Смешанные нотации (IDEF0 + BPMN)
   - Множество связей
   - Текстовые пояснения

3. **Текстовые страницы с инструкциями**
   - Описание процессов
   - Требования к моделированию
   - Справочные материалы

**Ключевые вызовы:**
- ❌ Векторная графика: отдельные элементы слишком малы (<1000 px²)
- ✅ Текст в диаграммах читаемый (native text layer)
- ✅ DeepSeek-OCR видит целую страницу → понимает контекст

---

## 🏗️ Архитектурное решение: Вариант А (без дообучения)

### Принцип: "Целая страница → Structured Output"

```
PDF Page → Render (300 DPI) → DeepSeek-OCR (BPMN prompt) → Structured Markdown → Parser → IR → BPMN XML
```

### Почему DeepSeek-OCR идеальна для задачи

#### 1. **SAM (Segment Anything Model)**
- ✅ Автоматически сегментирует фигуры: прямоугольники, ромбы, круги
- ✅ Определяет границы элементов
- ✅ Понимает иерархию: Pool → Lanes → Elements

#### 2. **CLIP (Vision-Language)**
- ✅ Семантическое понимание: "это задача", "это шлюз", "это событие"
- ✅ Контекстное понимание: "процесс закупок", "согласование бюджета"
- ✅ Связывает визуальные элементы с их смыслом

#### 3. **Сверточное сжатие**
- ✅ 10-20x сжатие **с сохранением структуры** (97% точность при 10x)
- ✅ Компактное представление всей диаграммы
- ✅ Не теряется информация о позициях и связях

#### 4. **DeepSeek-3B-MoE Decoder**
- ✅ Преобразование в структурированный markdown
- ✅ Управление промптами → custom output format
- ✅ Может следовать детальным инструкциям

---

## 📦 Реализованные компоненты

### 1. `pdf_to_context/ocr_service/prompts.py` ✅

**Класс OCRPrompts** - коллекция специализированных промптов:

```python
OCRPrompts.get_bpmn_diagram_prompt()
# → Извлекает: Tasks, Gateways, Events, Flows, Lanes

OCRPrompts.get_complex_diagram_prompt()
# → Извлекает: Elements, Connections, Annotations

OCRPrompts.get_table_prompt()
# → Извлекает: Headers, Rows, Merged cells

OCRPrompts.get_text_with_graphics_prompt()
# → Извлекает: Paragraphs + Graphics descriptions
```

**Принципы:**
- SOLID: Single Responsibility - только хранение промптов
- KISS: Простые, понятные шаблоны
- DRY: Переиспользуемые промпты

---

### 2. Обновленный `pdf_to_context/ocr_service/app.py` ✅

**Новый API:**

```python
POST /ocr/figure
  - file: PNG/JPEG изображение
  - prompt_type: 'default' | 'bpmn' | 'complex_diagram' | 'table' | 'text_graphics'

GET /prompts
  - Список доступных типов промптов с описаниями
```

**Что изменилось:**
- ✅ Поддержка кастомных промптов через параметр `prompt_type`
- ✅ Автоматический выбор промпта по типу контента
- ✅ Endpoint `/prompts` для документации API

---

### 3. `test_bpmn_pages.py` ✅

**Тестовый скрипт** для проверки на реальных примерах:

**Тестовые страницы:**
1. **Страница 54** - "Эксклюзивный шлюз по событиям"
   - Prompt: `bpmn`
   - Простая BPMN диаграмма
   
2. **Страница 26** - "Переход от IDEF0 к BPMN"
   - Prompt: `complex_diagram`
   - Сложная схема с разными нотациями

**Что делает:**
1. Проверяет доступность OCR сервиса
2. Рендерит страницы (300 DPI)
3. OCR обработка с разными промптами
4. Сохранение результатов:
   - PNG изображение
   - Markdown контент
   - JSON с полными данными

---

### 4. `DeepSeek-OCR/API_REFERENCE.md` ✅

**Полная документация API:**
- Endpoints с примерами
- Описание всех типов промптов
- Примеры использования (curl + Python)
- Troubleshooting
- Рекомендации по конфигурации

---

## 🧪 План тестирования

### Этап 1: Proof of Concept (1-2 часа)

**Шаг 1: Запуск OCR сервиса**
```bash
cd ~/Obligations
source DeepSeek-OCR/venv/bin/activate
python -m uvicorn pdf_to_context.ocr_service.app:app --host 0.0.0.0 --port 8000
```

**Шаг 2: Проверка доступности**
```bash
curl http://localhost:8000/health | jq .
curl http://localhost:8000/prompts | jq .
```

**Шаг 3: Запуск тестов**
```bash
python test_bpmn_pages.py
```

**Ожидаемый результат:**
- ✅ Распознан текст из элементов BPMN
- ✅ Структура диаграммы сохранена
- ✅ Связи между элементами извлечены

**Критерии успеха:**
- Точность распознавания текста: >90%
- Полнота извлечения элементов: >85%
- Сохранение структуры: связи между элементами видны

---

### Этап 2: Оценка качества (2-4 часа)

**Тестовая выборка:**
- 10-15 страниц с разными типами BPMN диаграмм
- Простые → сложные
- С разным количеством элементов (3-50+)

**Метрики:**
1. **Precision (точность):** % правильно распознанных элементов
2. **Recall (полнота):** % найденных элементов от общего числа
3. **Structure preservation:** сохранение связей между элементами
4. **F1-score:** гармоническое среднее Precision и Recall

**Анализ:**
- Где модель справляется отлично?
- Где есть ошибки?
- Нужно ли дообучение?

---

### Этап 3: Интеграция в Pipeline (1-2 дня)

**Компоненты для разработки:**

1. **BPMNDetector** (определение типа страницы)
   ```python
   def is_bpmn_page(page_data) -> bool:
       # Эвристики:
       # - >50 векторных элементов
       # - Ключевые слова: процесс, шлюз, событие
   ```

2. **BPMNParser** (парсинг Markdown → IR)
   ```python
   def parse_markdown_to_bpmn_ir(markdown) -> BPMNIR:
       # Извлечение:
       # - Tasks, Gateways, Events
       # - Sequence Flows
       # - Lanes / Pools
   ```

3. **BPMNFormatter** (IR → BPMN XML)
   ```python
   def format_to_xml(bpmn_ir) -> str:
       # Генерация BPMN 2.0 XML
       # Совместимость с Camunda Modeler
   ```

4. **Обновленный Pipeline**
   ```python
   PDF → Native Extract → BPMN Detect → (if BPMN):
       → Render page → DeepSeek-OCR (bpmn prompt)
       → Parse → BPMN IR → BPMN XML
   ```

---

## 📊 Ожидаемые результаты

### Без дообучения (Вариант А):

**Точность:** 92-97%
- ✅ Отлично для простых BPMN (1-15 элементов)
- ✅ Хорошо для средних BPMN (15-30 элементов)
- ⚠️ Удовлетворительно для сложных BPMN (30+ элементов)

**Время обработки:**
- Рендеринг страницы (300 DPI): ~0.5-1s
- OCR DeepSeek (RTX 5080): ~5-15s
- Парсинг Markdown → IR: ~0.1-0.5s
- **Итого:** ~6-17 секунд на страницу

**Ограничения:**
- Может не распознать сильно зашумленные диаграммы
- Сложные наложения элементов могут вызвать ошибки
- Нестандартные обозначения требуют дообучения

---

### С дообучением (Вариант B, опционально):

**Точность:** 97-99%+
- ✅ Отлично для всех типов BPMN
- ✅ Понимание специфической терминологии
- ✅ Адаптация к нестандартным обозначениям

**Требуемые ресурсы:**
- Датасет: 50-100 аннотированных примеров
- Время аннотации: 40-80 часов
- Обучение: 2-4 часа (RTX 5080)
- Разработка: 1-2 недели

**Рекомендация:** Начать с Варианта А, оценить качество, затем решить о дообучении.

---

## 💡 Следующие шаги

### Сейчас (готово к тестированию):

1. ✅ OCR сервис с BPMN промптами
2. ✅ Тестовый скрипт на примерах
3. ✅ API документация

### После тестирования (если качество достаточно):

1. **Разработать BPMNDetector**
   - Автоматическое определение страниц с BPMN
   - Классификация: простая/средняя/сложная диаграмма

2. **Разработать BPMNParser**
   - Парсинг Markdown → структурированный IR
   - Извлечение элементов, связей, ролей

3. **Разработать BPMNFormatter**
   - IR → BPMN 2.0 XML
   - Генерация координат для визуализации
   - Валидация в Camunda Modeler

4. **Интегрировать в Pipeline**
   - Обновить PDFToContextPipeline
   - Добавить режим "BPMN extraction"

5. **Добавить фильтрацию страниц**
   - Пользователь может пометить страницы как "не нужны"
   - Пропуск титульных, оглавления, справочных страниц

---

## 🎯 Критерии принятия решения о дообучении

### ✅ Дообучение НЕ нужно, если:
- Точность >90% на тестовой выборке
- Структура диаграмм сохраняется
- Редкие ошибки можно исправить постобработкой

### ⚠️ Дообучение ЖЕЛАТЕЛЬНО, если:
- Точность 80-90%
- Есть систематические ошибки (например, путает типы гейтвеев)
- Не распознает специфическую терминологию

### 🔴 Дообучение НЕОБХОДИМО, если:
- Точность <80%
- Не понимает структуру диаграмм
- Теряет связи между элементами

---

## 📚 Ссылки

### Документация проекта:
- `README.md` - общее описание
- `docs/Architecture.md` - техническая архитектура
- `docs/DeepSeek_OCR_Setup.md` - установка DeepSeek-OCR
- `DeepSeek-OCR/API_REFERENCE.md` - API документация

### DeepSeek-OCR:
- GitHub: https://github.com/deepseek-ai/DeepSeek-OCR
- HuggingFace: https://huggingface.co/deepseek-ai/DeepSeek-OCR
- Paper: https://arxiv.org/abs/2409.12014

### Тестирование:
- Тестовый скрипт: `test_bpmn_pages.py`
- Промпты: `pdf_to_context/ocr_service/prompts.py`
- OCR сервис: `pdf_to_context/ocr_service/app.py`

---

**Готово к тестированию!** 🚀

Запустите `test_bpmn_pages.py` и оцените качество распознавания на страницах 54 и 26.








